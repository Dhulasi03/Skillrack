// a=1;A=1
System.out.print((ch >= 'A' && ch <= 'Z') ? (ch - 'A' + 1) : (ch - 'a' + 1));

///last three string  ends with
  if (word.endsWith("ion"))

///concat
str1.concat(str2)


///contains
str1.contains(str2)

///char array  to  string
String.valueOf(charArray)
 Summary
Data Type	Method
char[] → String	new String(charArray) or String.valueOf(charArray)
int[] → String	Arrays.toString(intArray)
String[] → String	String.join(" ", array) or StringJoiner
ArrayList<String> → String	String.join(" ", list)

//// repeat the string
str.repeat(N)

